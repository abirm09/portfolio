import NavBar from "./navbar";

const Header = () => {
  return (
    <header className="bg-slate-500/20 py-2 backdrop-blur-[10px] shadow-sm">
      <NavBar />
    </header>
  );
};

export default Header;
